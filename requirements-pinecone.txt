# Optional Pinecone sync stage. Install into the main .venv (no GPU/torch
# needed here -- this only reads already-generated vectors and metadata):
#   .\.venv\Scripts\python.exe -m pip install -r requirements-pinecone.txt
-r requirements.txt
pinecone==10.0.0
